# 채팅 탭 개선 계획 (Phase 1 - Day 8)

**작성일**: 2026-01-23  
**대상 파일**: `lib/features/chat/screens/chat_list_screen.dart`  
**참조 디자인**: 디자인 시스템 및 공통 컴포넌트

---

## 📋 목차

1. [현재 코드 분석](#1-현재-코드-분석)
2. [개선 목표](#2-개선-목표)
3. [상세 작업 계획](#3-상세-작업-계획)
4. [단계별 작업 순서](#4-작업-순서)

---

## 1. 현재 코드 분석

### 1.1 구조
- **ChatListScreen**: 상태 관리 및 수명 주기 처리
- **_ChatListContent**: 실제 UI 구현
- **_ModernChatListItem**: 개별 채팅방 아이템 (이미 어느 정도 스타일링 되어 있음)

### 1.2 문제점
1. **디자인 시스템 미적용**:
   - 하드코딩된 색상 (`kChatListPrimaryGreen` 등) 사용
   - `AppTypography`, `AppSpacing` 미사용
2. **빈 상태(Empty State)**:
   - 단순한 아이콘 + 텍스트
   - 시각적으로 심심함
3. **한의사 찾기 버튼**:
   - 하단에 고정된 `ElevatedButton`
   - 리스트가 길어지면 공간을 차지함 -> FAB로 변경 필요

---

## 2. 개선 목표

### 2.1 디자인 시스템 통합
✅ **색상**: `AppColors` 사용 (Brand, Primary, Secondary)  
✅ **타이포그래피**: `AppTypography` 사용  
✅ **간격**: `AppSpacing` 사용  
✅ **컴포넌트**: `AppCountBadge`, `AppBaseCard` 등 활용

### 2.2 UI 개선
✅ **채팅 리스트 아이템**:
   - 프로필 이미지 강조
   - 읽지 않은 메시지 수 배지 개선 (`AppCountBadge`)
   - 마지막 메시지 시간 포맷 통일
✅ **빈 상태**:
   - 더 매력적인 일러스트레이션 (아이콘 조합)
   - 명확한 CTA 버튼 (`AppPrimaryButton`)
✅ **FAB (Floating Action Button)**:
   - "새 상담" 버튼을 우측 하단 FAB로 변경

---

## 3. 상세 작업 계획

### Step 1: Import 추가
```dart
import '../../../shared/theme/app_colors.dart';
import '../../../shared/theme/app_typography.dart';
import '../../../shared/theme/app_spacing.dart';
import '../../../shared/theme/app_radius.dart';
import '../../../shared/theme/app_shadows.dart';
import '../../../shared/widgets/common_badge.dart';
import '../../../shared/widgets/common_button.dart';
import '../../../shared/widgets/common_card.dart';
```

### Step 2: 하드코딩된 색상 제거
- `kChatListPrimaryGreen` 등 기존 상수 제거
- `AppColors`로 대체

### Step 3: _ChatListContent 개선 (빈 상태 & FAB)
- `build` 메서드 내 빈 상태 UI 개선
- 하단 버튼 제거하고 `Scaffold`의 `floatingActionButton`으로 이동

### Step 4: _ModernChatListItem 개선
- `Container` → `AppBaseCard` (또는 스타일링된 Container)
- 텍스트 스타일 `AppTypography`로 교체
- 배지 `AppCountBadge`로 교체

---

## 4. 작업 순서

1. **Import 추가 및 상수 정리** (5분)
2. **빈 상태 UI 개선** (10분)
3. **FAB 추가 및 리스트 구조 변경** (10분)
4. **리스트 아이템 디자인 개선** (15분)
5. **테스트** (5분)

**총 예상 시간**: 45분

---

**작업을 시작할까요?** 🚀

