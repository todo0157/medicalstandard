# 주소 검색 개선 사항

## 🔧 적용된 개선 사항

### 1. 디바운싱 (Debouncing) 추가 ✅

**문제**: 사용자가 "서울특별시"를 입력할 때마다 즉시 API 요청이 발생:
- "서" 입력 → API 요청
- "서울" 입력 → API 요청
- "서울특" 입력 → API 요청
- "서울특별" 입력 → API 요청
- "서울특별시" 입력 → API 요청

**해결**: 사용자가 입력을 멈춘 후 **500ms** 후에만 API 요청을 보냅니다.

**효과**:
- 불필요한 API 요청 감소
- API 할당량 절약
- 네트워크 트래픽 감소
- 더 나은 사용자 경험

### 2. 최소 입력 길이 제한 ✅

**문제**: 1자만 입력해도 API 요청이 발생하여 의미 없는 검색 결과 반환

**해결**: **최소 2자 이상** 입력해야 검색이 시작됩니다.

**효과**:
- 의미 있는 검색 결과만 표시
- API 할당량 절약
- 더 나은 사용자 경험

### 3. Provider 레벨 검증 추가 ✅

`address_provider.dart`에서도 최소 길이 검증을 추가하여 이중 보호를 제공합니다.

---

## 📊 개선 전후 비교

### 개선 전:
```
사용자 입력: "서울특별시"
→ "서" 입력 → API 요청 (결과 없음)
→ "서울" 입력 → API 요청 (부분 결과)
→ "서울특" 입력 → API 요청 (부분 결과)
→ "서울특별" 입력 → API 요청 (부분 결과)
→ "서울특별시" 입력 → API 요청 (정확한 결과)
총 5번의 API 요청
```

### 개선 후:
```
사용자 입력: "서울특별시"
→ "서" 입력 → 검색 안 함 (최소 길이 미만)
→ "서울" 입력 → 500ms 대기
→ "서울특" 입력 → 이전 타이머 취소, 500ms 대기
→ "서울특별" 입력 → 이전 타이머 취소, 500ms 대기
→ "서울특별시" 입력 → 이전 타이머 취소, 500ms 대기
→ 500ms 후 → API 요청 (정확한 결과)
총 1번의 API 요청
```

---

## 🎯 추가 개선 가능 사항

### 1. 최소 입력 길이 조정

현재는 2자로 설정되어 있지만, 필요에 따라 3자로 조정할 수 있습니다:

```dart
static const int _minQueryLength = 3; // 3자로 변경
```

### 2. 디바운스 시간 조정

현재는 500ms로 설정되어 있지만, 필요에 따라 조정할 수 있습니다:

```dart
static const Duration _debounceDuration = Duration(milliseconds: 300); // 300ms로 변경
```

### 3. 검색 결과 캐싱

같은 검색어를 다시 검색할 때 캐시된 결과를 사용할 수 있습니다.

---

## 🧪 테스트 방법

1. **디바운싱 테스트**:
   - 주소 검색 화면에서 "서울특별시"를 빠르게 입력
   - 서버 로그에서 API 요청이 1번만 발생하는지 확인

2. **최소 길이 테스트**:
   - 주소 검색 화면에서 "서"만 입력
   - "최소 2자 이상 입력해주세요" 메시지가 표시되는지 확인
   - API 요청이 발생하지 않는지 확인

3. **정상 검색 테스트**:
   - 주소 검색 화면에서 "서울특별시"를 입력하고 500ms 이상 대기
   - 검색 결과가 정상적으로 표시되는지 확인

---

## 📝 코드 변경 사항

### `lib/features/address/screens/address_search_screen.dart`

1. **디바운싱 타이머 추가**:
   ```dart
   Timer? _debounceTimer;
   String _debouncedQuery = '';
   ```

2. **최소 입력 길이 상수 추가**:
   ```dart
   static const int _minQueryLength = 2;
   static const Duration _debounceDuration = Duration(milliseconds: 500);
   ```

3. **입력 변경 핸들러 개선**:
   - 최소 길이 검증
   - 디바운싱 타이머 관리
   - 이전 타이머 취소

### `lib/core/providers/address_provider.dart`

1. **Provider 레벨 검증 추가**:
   - 최소 길이 검증
   - 빈 쿼리 검증

---

**이제 주소 검색이 더 효율적으로 작동합니다!** 🚀


