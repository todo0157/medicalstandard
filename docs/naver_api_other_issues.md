# 네이버 지도 API 기타 문제 해결 가이드

## 🔍 API 키/구독 외 다른 원인들

### 원인 1: Application 서비스 환경 설정 문제 ⚠️

**문제**: Application의 "서비스 환경"에서 Web 서비스 URL이 설정되지 않았거나 잘못 설정됨

**해결 방법**:
1. 네이버 클라우드 플랫폼 콘솔 → Maps → Application → "hanbang" 클릭
2. **"서비스 환경"** 또는 **"Service Environment"** 탭 클릭
3. **"Web 서비스 URL"** 확인:
   - 개발 환경: `http://localhost:5173` 또는 `http://localhost:8080`
   - 프로덕션 환경: 실제 도메인 (예: `https://yourdomain.com`)
4. URL이 비어있거나 잘못되었다면 올바른 URL 입력
5. **"저장"** 또는 **"Save"** 버튼 클릭

**참고**: 일부 API는 서비스 환경 URL이 설정되어 있어야 정상 작동할 수 있습니다.

---

### 원인 2: API 엔드포인트 URL 오류

**현재 사용 중인 URL**:
```
https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode
```

**확인 사항**:
1. 네이버 클라우드 플랫폼 콘솔에서 공식 API 문서 확인
2. 엔드포인트 URL이 최신 버전인지 확인
3. 리전(Region) 설정이 올바른지 확인

**대안 URL 시도**:
```typescript
// 대안 1: 다른 리전 엔드포인트
const naverApiUrl = 'https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode';

// 대안 2: API Gateway 엔드포인트 (만약 다르다면)
// 네이버 클라우드 플랫폼 콘솔에서 확인한 정확한 엔드포인트 사용
```

---

### 원인 3: 헤더 형식 문제

**현재 사용 중인 헤더**:
```typescript
'X-NCP-APIGW-API-KEY-ID': clientId
'X-NCP-APIGW-API-KEY': clientSecret
```

**확인 사항**:
1. 헤더 이름이 정확한지 확인 (대소문자 구분)
2. 헤더 값에 공백이나 특수 문자가 포함되지 않았는지 확인
3. Content-Type 헤더가 GET 요청에서 문제를 일으키는지 확인

**해결 방법**:
- GET 요청에서는 Content-Type 헤더를 제거하는 것이 더 안전할 수 있음
- 일부 API Gateway는 GET 요청에 Content-Type을 허용하지 않을 수 있음

---

### 원인 4: 리전(Region) 불일치

**문제**: API 키가 특정 리전에 속해 있는데 다른 리전의 엔드포인트를 사용

**해결 방법**:
1. 네이버 클라우드 플랫폼 콘솔에서 현재 리전 확인
2. Application이 생성된 리전 확인
3. API 엔드포인트가 해당 리전과 일치하는지 확인

---

### 원인 5: API Gateway 설정 문제

**문제**: Application이 API Gateway를 통해 접근해야 하는데 직접 엔드포인트를 사용

**해결 방법**:
1. 네이버 클라우드 플랫폼 콘솔에서 Application의 API Gateway 설정 확인
2. API Gateway 엔드포인트가 있다면 해당 엔드포인트 사용
3. API Gateway 인증 설정 확인

---

### 원인 6: 네트워크/프록시 문제

**문제**: 서버에서 네이버 API 서버로의 네트워크 연결 문제

**해결 방법**:
1. 서버에서 네이버 API 엔드포인트 접근 가능한지 확인:
   ```powershell
   curl https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode?query=서울
   ```

2. 방화벽 설정 확인:
   - 회사 네트워크를 사용 중이라면 방화벽에서 차단될 수 있음
   - VPN을 사용 중이라면 VPN 설정 확인

3. 프록시 설정 확인:
   - 서버가 프록시를 통해 인터넷에 접근하는 경우 프록시 설정 필요

---

### 원인 7: API 버전 문제

**문제**: 사용 중인 API 버전이 더 이상 지원되지 않거나 변경됨

**해결 방법**:
1. 네이버 클라우드 플랫폼 콘솔에서 최신 API 문서 확인
2. API 버전이 최신인지 확인
3. 엔드포인트 URL에 버전 정보가 포함되어 있는지 확인

---

### 원인 8: Application 타입 불일치

**문제**: Application이 Web 타입인데 Mobile 타입으로 설정되어 있거나 그 반대

**해결 방법**:
1. 네이버 클라우드 플랫폼 콘솔 → Maps → Application → "hanbang" 클릭
2. Application 타입 확인
3. 서버에서 사용하는 경우 Web 타입이어야 함
4. 타입이 잘못되었다면 올바른 타입으로 변경

---

## 🔧 디버깅 방법

### 1. 서버 로그 확인

서버 로그에서 다음을 확인:
```
[Naver Map API] Request URL: ...
[Naver Map API] Request headers: ...
[Naver Map API] Error: ...
```

### 2. 네이버 API 직접 테스트

Postman이나 curl을 사용하여 네이버 API를 직접 테스트:

```bash
curl -X GET "https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode?query=서울" \
  -H "X-NCP-APIGW-API-KEY-ID: YOUR_CLIENT_ID" \
  -H "X-NCP-APIGW-API-KEY: YOUR_CLIENT_SECRET"
```

### 3. 네이버 클라우드 플랫폼 콘솔에서 API 테스트

일부 네이버 클라우드 플랫폼 콘솔에서는 API를 직접 테스트할 수 있는 기능을 제공합니다.

---

## 📋 체크리스트

다음 사항을 모두 확인하세요:

- [ ] Application의 "서비스 환경"에서 Web 서비스 URL이 올바르게 설정되어 있는가?
- [ ] API 엔드포인트 URL이 최신이고 올바른가?
- [ ] 헤더 형식이 정확한가? (대소문자, 공백 등)
- [ ] 리전(Region) 설정이 올바른가?
- [ ] API Gateway 설정이 올바른가?
- [ ] 네트워크 연결이 정상적인가?
- [ ] API 버전이 최신인가?
- [ ] Application 타입이 올바른가? (Web/Mobile)

---

## 🆘 여전히 문제가 발생한다면

1. **네이버 클라우드 플랫폼 고객 지원 문의**
   - 위의 모든 사항을 확인했음에도 문제가 지속된다면
   - Trace ID와 함께 문의: 서버 로그에서 확인한 `x-ncp-trace-id` 값

2. **대안 API 사용 고려**
   - 카카오 주소 검색 API: `docs/address_api_alternatives.md` 참고
   - 공공데이터포털 주소 검색 API: `docs/address_api_alternatives.md` 참고

---

**가장 흔한 원인은 Application의 서비스 환경 설정 문제입니다. 먼저 확인해보세요!** 🚀


