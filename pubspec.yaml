name: hanbang_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # ▼▼▼ [수정됨] 이 블록이 누락되었습니다. ▼▼▼
  flutter_localizations:
    sdk: flutter
  # ▲▲▲ [수정됨] 이 블록을 추가했습니다. ▲▲▲

  kakao_flutter_sdk: ^1.9.5
  intl: 0.20.2  # 날짜 포맷팅용 (추가 필요)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true